import { GRCLayout } from "@/components/grc/GRCLayout";

export const metadata = {
  title: "GRC | rootaccess",
  description: "Policies, evidence, and accountability — governance, risk, and compliance.",
};

export default function GRCWorkflowPage() {
  return <GRCLayout />;
}
