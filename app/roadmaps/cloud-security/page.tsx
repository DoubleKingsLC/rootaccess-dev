import { CloudSecurityLayout } from "../../../components/cloud-security/CloudSecurityLayout";

export const metadata = {
  title: "Cloud Security | rootaccess",
  description: "S3 exposure. GuardDuty detection. Secure IaC rebuild.",
};

export default function CloudSecurityPage() {
  return <CloudSecurityLayout />;
}
